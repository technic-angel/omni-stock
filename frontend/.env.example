### Frontend environment example (Vite)
### Copy this to `frontend/.env` for local development and replace with real values.
### DO NOT commit real secrets. Keep `frontend/.env` in .gitignore.

# Supabase public config for uploads
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=pk.your_public_anon_key_here

# Backend API URL used by frontend during development
# Backend runs on port 4000, frontend on port 3000
VITE_API_BASE=http://localhost:4000/api
VITE_API_BASE_PROD=https://your-backend.onrender.com/api
